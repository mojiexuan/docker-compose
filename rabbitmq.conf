# 内存限制配置（推荐方式）
vm_memory_high_watermark.absolute = 1GB

# 磁盘警告阈值（50MB）
disk_free_limit.absolute = 50000000

# 默认vhost配置
default_vhost = /

# 其他推荐配置（可选）
channel_max = 2047
heartbeat = 60
default_user = rabbit
default_pass = 123456